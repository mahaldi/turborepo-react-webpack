{
  "name": "ui",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "sideEffects": false,
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup src/index.jsx --format cjs --dts --external react",
    "clean": "rm -rf dist",
    "dev": "tsup src/index.jsx --format cjs --watch --dts --external react",
    "lint": "eslint src/**/*"
  },
  "devDependencies": {
    "eslint": "^8.4.1",
    "react": "^17.0.1",
    "tsup": "^5.10.1"
  }
}
